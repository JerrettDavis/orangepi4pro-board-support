name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y shellcheck gcc libusb-1.0-0-dev libx11-dev libxtst-dev

      - name: Run CI checks
        run: scripts/ci-checks.sh

      - name: Build source archive
        run: |
          repo="${GITHUB_REPOSITORY#*/}"
          tar --exclude=.git -czf "${repo}-${GITHUB_REF_NAME}.tar.gz" .

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          repo="${GITHUB_REPOSITORY#*/}"
          gh release create "$GITHUB_REF_NAME" "${repo}-${GITHUB_REF_NAME}.tar.gz" --generate-notes

