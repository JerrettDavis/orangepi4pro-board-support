# Security

Do not commit:

- private keys, GitHub tokens, Wi-Fi credentials, or service credentials;
- private captures from `/etc`, `/home`, `/var/log`, browser profiles, or SSH
  configuration;
- firmware or bootloader binaries without provenance and checksum review.

Report security concerns privately to the repository owner before filing public
issues.

