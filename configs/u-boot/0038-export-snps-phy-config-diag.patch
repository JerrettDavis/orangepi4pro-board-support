diff --git a/drivers/video/drm/sunxi_device/hardware/lowlevel_hdmi20/phy_snps.c b/drivers/video/drm/sunxi_device/hardware/lowlevel_hdmi20/phy_snps.c
--- a/drivers/video/drm/sunxi_device/hardware/lowlevel_hdmi20/phy_snps.c
+++ b/drivers/video/drm/sunxi_device/hardware/lowlevel_hdmi20/phy_snps.c
@@ -7,6 +7,7 @@
  ******************************************************************************/
 #include <dm.h>
+#include <common.h>
 
 #include "dw_dev.h"
 #include "dw_phy.h"
@@ -748,22 +749,43 @@ int snps_phy_config(void)
 {
 	int ret = 0;
+	int ret_init = 0;
+	int ret_mpll = 0;
+	int ret_drive = 0;
+	int ret_wait = 0;
+	u8 pre_phy = 0;
+	u8 init_phy = 0;
+	u8 power_phy = 0;
+	u8 final_phy = 0;
+	u8 final_lock = 0;
+	u8 final_rst = 0;
+	u8 final_clkdis = 0;
+	char diag[128];
 
-	_snps_phy_config_init();
+	pre_phy = (u8)dw_read(PHY_STAT0);
+	ret_init = _snps_phy_config_init();
+	init_phy = (u8)dw_read(PHY_STAT0);
 
-	ret = _snps_phy_cfg_mpll();
-	if (ret != 0) {
+	ret_mpll = _snps_phy_cfg_mpll();
+	if (ret_mpll != 0) {
 		hdmi_err("snps phy config mpll failed\n");
 		goto failed_exit;
 	}
-	ret = _snps_phy_cfg_drive();
-	if (ret != 0) {
+	ret_drive = _snps_phy_cfg_drive();
+	if (ret_drive != 0) {
 		hdmi_err("snps phy config drive failed\n");
 		goto failed_exit;
 	}
 
 	dw_phy_set_power(DW_HDMI_ENABLE);
+	power_phy = (u8)dw_read(PHY_STAT0);
 
-	ret = dw_phy_wait_lock();
-	hdmi_inf("snps phy state: %s\n", ret == 1 ? "lock" : "unlock");
-	if (ret == 1)
-		return 0;
+	ret_wait = dw_phy_wait_lock();
+	hdmi_inf("snps phy state: %s\n", ret_wait == 1 ? "lock" : "unlock");
+	ret = (ret_wait == 1) ? 0 : -1;
+	goto diag_exit;
 
 failed_exit:
-	return -1;
+	ret = -1;
+diag_exit:
+	final_phy = (u8)dw_read(PHY_STAT0);
+	final_lock = (u8)dw_read(MC_LOCKONCLOCK);
+	final_rst = (u8)dw_read(MC_PHYRSTZ);
+	final_clkdis = (u8)dw_read(MC_CLKDIS);
+	snprintf(diag, sizeof(diag),
+		 "snps=ret%d,init%d,mpll%d,drive%d,wait%d,pre%02x,initphy%02x,power%02x,phy%02x,lock%02x,rst%02x,clkdis%02x",
+		 ret, ret_init, ret_mpll, ret_drive, ret_wait, pre_phy, init_phy, power_phy,
+		 final_phy, final_lock, final_rst, final_clkdis);
+	env_set("opi_snps_phy_diag", diag);
+	return ret;
 }
 
 int snps_phy_write(u8 addr, void *data)
