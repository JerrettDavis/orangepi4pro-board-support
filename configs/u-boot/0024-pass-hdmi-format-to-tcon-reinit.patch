diff --git a/drivers/video/drm/sunxi_drm_hdmi.c b/drivers/video/drm/sunxi_drm_hdmi.c
index 060fca1..c9db28b 100644
--- a/drivers/video/drm/sunxi_drm_hdmi.c
+++ b/drivers/video/drm/sunxi_drm_hdmi.c
@@ -697,6 +697,8 @@ static int _sunxi_hdmi_reinit_active_display(void)
 	memcpy(&disp_cfg.timing, &hdmi->disp_timing, sizeof(struct disp_video_timings));
 	disp_cfg.type = INTERFACE_HDMI;
 	disp_cfg.de_id = hw_id;
+	disp_cfg.format = hdmi->disp_config.format;
+	disp_cfg.sw_enable = 0;
 	disp_cfg.irq_handler = scrtc_state->crtc_irq_handler;
 	disp_cfg.irq_data = state;
 
@@ -739,13 +741,14 @@ static int _sunxi_hdmi_reinit_active_display(void)
 	fc_gcp = (u8)dw_read(FC_GCP);
 
 	snprintf(diag, sizeof(diag),
-		 "reinit=d%d,x%d,m%d,t%d,s%d,i%d,n%d,r%d,c%d,drv%d,drm%d,ms%d,mode%dx%d,clk%u,top%08x_%08x_%08x_%08x_%08x_%08x_%08x_%08x,core%02x%02x%02x%02x%02x%02x",
+		 "reinit=d%d,x%d,m%d,t%d,s%d,i%d,n%d,r%d,c%d,drv%d,drm%d,ms%d,fmt%u,sw%d,mode%dx%d,clk%u,top%08x_%08x_%08x_%08x_%08x_%08x_%08x_%08x,core%02x%02x%02x%02x%02x%02x",
 		 ret_dis_drv, ret_tcon_exit, ret_disp_mode, ret_timing,
 		 ret_select, ret_disp_info, ret_tcon_init, ret_rate, ret_config,
 		 hdmi->hdmi_ctrl.drv_enable, hdmi->hdmi_ctrl.drm_enable,
 		 hdmi->hdmi_ctrl.drm_mode_set,
+		 hdmi->disp_config.format, disp_cfg.sw_enable,
 		 hdmi->drm_mode.hdisplay, hdmi->drm_mode.vdisplay,
 		 hdmi->disp_timing.pixel_clk, top_cfg, top_stat, top_pll, top_sel,
 		 top_ldo, top_pat0, top_pat1, top_lockreg,
@@ -780,6 +783,8 @@ int _sunxi_drm_hdmi_enable(struct sunxi_drm_connector *conn,
 	memcpy(&disp_cfg.timing, &hdmi->disp_timing, sizeof(struct disp_video_timings));
 	disp_cfg.type  = INTERFACE_HDMI;
 	disp_cfg.de_id = hw_id;
+	disp_cfg.format = hdmi->disp_config.format;
+	disp_cfg.sw_enable = 0;
 	disp_cfg.irq_handler = scrtc_state->crtc_irq_handler;
 	disp_cfg.irq_data = state;
 
